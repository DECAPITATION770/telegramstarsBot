services:
  bot:
    build: .
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-}
      ADMIN_IDS: ${ADMIN_IDS:-}
      WEB_SERVER_HOST: 0.0.0.0
      WEB_SERVER_PORT: 8080
      WEBHOOK_PATH: /webhook
    ports:
      - "8080:8080"
